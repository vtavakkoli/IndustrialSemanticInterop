# Adaptive Strategy Selection

The `adaptive_selection` strategy is a rule-based policy layer that selects among:
- `ontology_based`
- `direct_translation`
- `soa`
- `opcua_mediated`

It logs selected strategy, policy, decision reason, scenario features, fallback use, and policy overhead. This is standards-informed and representative, not full standards conformance validation.
